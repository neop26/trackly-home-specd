# Example Edge Function env for local `supabase functions serve`
#
# NOTE: On Supabase hosted, secrets cannot start with SUPABASE_.
# Use SB_* in hosted environments.

SB_URL=http://127.0.0.1:54321
SB_ANON_KEY=sb_publishable_...
SB_SERVICE_ROLE_KEY=sb_secret_...

# Used for invite link generation + CORS allowlist
SITE_URL=http://localhost:5173

# Used to hash invite tokens (set a strong random value in hosted)
INVITE_TOKEN_SECRET=dev-only-secret

# Optional (only if sending emails in hosted)
RESEND_API_KEY=
RESEND_FROM=

# Optional CSV allowlist (localhost is always allowed in code)
CORS_ORIGINS=http://localhost:5173
